# Language / 言語 / 语言
**[English](../en/user-guide.md)** | **[日本語](../ja/user-guide.md)** | **[简体中文](../zh-CN/user-guide.md)** | **[繁體中文](../zh-TW/user-guide.md)**

---

# 📚 Multi Git Manager - 使用者指南

## 🌟 概述

Multi Git Manager 是一個在 Obsidian 內高效管理多個 Git 儲存庫的外掛。可以統一管理複雜的專案結構和多個筆記管理。

## 🎯 主要功能

### 🔍 自動儲存庫偵測
- **Vault 本體**: Obsidian Vault 本身是 Git 儲存庫的情況
- **父目錄**: Vault 外的專案根目錄
- **子資料夾**: Vault 內的獨立專案

### 📊 即時監控
- **狀態列顯示**: 持續監控修改檔案數量
- **30秒間隔更新**: 自動狀態同步
- **視覺化指示器**: 立即了解是否有變更

### 🎛️ 統一管理介面
- **Git Manager View**: 所有儲存庫的統一管理介面
- **批次操作**: 多個儲存庫的同時操作
- **選擇性執行**: 僅針對需要的儲存庫

## 🖥️ 使用者介面

### 1. 狀態列
```
[Git: 5 changes]  <-- 在這裡顯示
```
- **顯示內容**: 所有儲存庫的修改檔案總數
- **點擊**: 開啟 Git Manager View
- **更新頻率**: 30秒間隔

### 2. 功能區圖示
```
🌳 Git Manager  <-- 左側欄的圖示
```
- **功能**: Git Manager View 的開關
- **顯示**: 始終可用
- **工具提示**: "Multi Git Manager"

### 3. 命令選擇器
`Ctrl/Cmd + P` 可使用的命令:

| 命令 | 功能 | 快速鍵 |
|----------|------|----------------|
| **Open Git Manager** | 開啟主管理介面 | - |
| **Show Git Status** | 詳細狀態顯示 | - |
| **Git Commit** | 批次提交介面 | - |
| **Git Push** | 推送執行介面 | - |
| **Git Pull** | 拉取執行介面 | - |

## 📋 詳細功能說明

### Git Manager View

#### 📊 儲存庫概覽
```
┌─────────────────────────────────────┐
│ 🔄 Git Repository Manager          │
├─────────────────────────────────────┤
│ 📁 MyVault (3 changes)             │
│   • Modified: 2 files              │
│   • Untracked: 1 file              │
│   • Branch: main ↑1 ↓0             │
├─────────────────────────────────────┤
│ 📁 ParentProject (1 change)        │
│   • Modified: 1 file               │
│   • Branch: develop ↑0 ↓2          │
└─────────────────────────────────────┘
```

#### 🎮 控制按鈕
- **🔄 Refresh**: 手動狀態更新
- **📝 Commit All**: 所有儲存庫批次提交
- **⬆️ Push All**: 所有儲存庫推送
- **⬇️ Pull All**: 所有儲存庫拉取

### Git Status Modal

#### 📄 詳細顯示介面
```
┌─────────────────────────────────────┐
│ Git Status - All Repositories      │
├─────────────────────────────────────┤
│ 📂 MyVault                         │
│   Branch: main                     │
│   ↑1 ↓0                           │
│                                    │
│   Modified (2):                    │
│     M notes/daily/2023-10-24.md   │
│     M templates/meeting.md         │
│                                    │
│   Untracked (1):                   │
│     ? assets/image.png             │
├─────────────────────────────────────┤
│ 📂 ParentProject (Parent)          │
│   Branch: develop                  │
│   ↑0 ↓2                           │
│                                    │
│   Modified (1):                    │
│     M README.md                    │
└─────────────────────────────────────┘
```

### Git Commit Modal

#### ✏️ 提交建立介面
```
┌─────────────────────────────────────┐
│ Git Commit                          │
├─────────────────────────────────────┤
│ Select Repositories:                │
│   ☑️ MyVault - 3 changes           │
│   ☑️ ParentProject - 1 change      │
│   ☐ EmptyRepo - 0 changes          │
├─────────────────────────────────────┤
│ Commit Message:                     │
│ ┌─────────────────────────────────┐ │
│ │ Update daily notes and fix bugs │ │
│ │                                 │ │
│ │                                 │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│           [Commit] [Cancel]         │
└─────────────────────────────────────┘
```

### Git Push/Pull Modal

#### 🔄 同步操作介面
```
┌─────────────────────────────────────┐
│ Git Push                            │
├─────────────────────────────────────┤
│ Select Repositories:                │
│   ☑️ MyVault                       │
│   ☑️ ParentProject (Parent)        │
│   ☐ LocalOnlyRepo                  │
├─────────────────────────────────────┤
│             [Push] [Cancel]         │
└─────────────────────────────────────┘
```

## 🔧 進階使用方法

### 💼 工作流程範例

#### 1. 論文撰寫專案
```
📁 ResearchProject/
├── 🔄 obsidian-vault/ (備忘・想法)
├── 🔄 latex-manuscript/ (論文原稿)
├── 🔄 data-analysis/ (分析程式碼)
└── 🔄 bibliography/ (參考文獻)
```

**運行流程**:
1. **日常工作**: 在各儲存庫進行獨立工作
2. **階段性提交**: 使用 Multi Git Manager 批次提交
3. **團隊分享**: 選擇性推送進行部分分享

#### 2. 部落格文章管理
```
📁 BlogVault/
├── 🔄 drafts/ (草稿儲存庫)
├── 🔄 published/ (發布文章)
├── 🔄 assets/ (圖像・資源)
└── 🔄 tools/ (自動化指令碼)
```

**運行流程**:
1. **建立草稿**: 在 drafts/ 中撰寫文章
2. **審核**: 提交供團隊確認
3. **發布準備**: 移動到 published/ 並整理
4. **批次部署**: 同步所有儲存庫

#### 3. 學習筆記管理
```
📁 StudyVault/
├── 🔄 computer-science/ (CS相關)
├── 🔄 mathematics/ (數學)
├── 🔄 projects/ (實習專案)
└── 🔄 resources/ (共享資源)
```

### 🎨 自訂設定

#### CSS 樣式
```css
/* 自訂 styles.css */
.git-manager-view {
    font-family: 'JetBrains Mono', monospace;
}

.git-file-modified {
    color: #ff9500;
}

.git-file-added {
    color: #00ff00;
}

.git-file-deleted {
    color: #ff4757;
}
```

#### 自動更新間隔調整
```typescript
// main.ts 的相應部分
this.registerInterval(
    window.setInterval(() => this.updateStatusBar(), 10000) // 改為10秒
);
```

## 🔍 故障排除

### 常見問題及解決方法

#### 1. 🚫 儲存庫未被偵測

**症狀**: Git Manager View 中不顯示儲存庫

**原因和解決方法**:
```bash
# 原因1: 未初始化為 Git 儲存庫
cd "[目標資料夾]"
git init

# 原因2: .git 資料夾被隱藏
ls -la  # 確認 .git 資料夾

# 原因3: 權限問題
chmod -R 755 .git
```

#### 2. ⚠️ Git 命令失敗

**症狀**: "git command not found" 錯誤

**解決方法**:
```bash
# Windows
where git
# 如果不顯示路徑，重新安裝 Git for Windows

# macOS  
brew install git
# 或安裝 Xcode Command Line Tools

# Linux
sudo apt install git  # Ubuntu/Debian
sudo yum install git   # CentOS/RHEL
```

#### 3. 🔄 狀態不更新

**症狀**: 修改檔案後狀態列不更新

**解決方法**:
1. **手動更新**: 點擊 Git Manager View 的🔄按鈕
2. **重新啟動外掛**: 在設定中關閉外掛後重新開啟
3. **重新啟動 Obsidian**: 完全重新啟動

#### 4. 🚨 效能問題

**症狀**: Obsidian 變慢

**原因**: 包含大量檔案的儲存庫

**解決方法**:
```bash
# 在 .gitignore 中排除應該忽略的檔案
echo "node_modules/" >> .gitignore
echo ".obsidian/workspace" >> .gitignore
echo "*.tmp" >> .gitignore
```

### 🛠️ 除錯方法

#### 1. 開發者工具確認
```javascript
// 用 Ctrl+Shift+I 開啟主控台
// 確認外掛日誌
console.log("Multi Git Manager"); // 用於搜尋
```

#### 2. 啟用詳細日誌
```javascript
// Obsidian設定 → 進階 → 開發者模式 設為開啟
// 顯示更詳細的錯誤日誌
```

#### 3. 檔案權限確認
```bash
# 確認外掛檔案權限
ls -la .obsidian/plugins/multi-git-manager/
# 確認 main.js, manifest.json 可讀取
```

## 📈 效能最佳化

### ⚡ 加速技巧

#### 1. 排除不必要的檔案
```gitignore
# .gitignore 建議設定
.obsidian/workspace
.obsidian/cache
*.tmp
*.log
node_modules/
.DS_Store
Thumbs.db
```

#### 2. 大型儲存庫分割
```
❌ 應避免的結構:
📁 HugeVault/ (10,000+ files)

✅ 建議結構:
📁 MainVault/ (日常備忘)
📁 ArchiveVault/ (過去資料)
📁 ProjectVault/ (專案)
```

#### 3. 更新頻率調整
- **重度使用者**: 10秒間隔
- **一般使用者**: 30秒間隔 (預設)
- **輕度使用者**: 60秒間隔

## 🔐 安全考慮事項

### 🛡️ 安全運行

#### 1. 機密資訊管理
```gitignore
# 排除機密檔案
*.key
*.pem
.env
secrets/
private/
```

#### 2. 遠端儲存庫設定
```bash
# 建議 HTTPS 連線
git remote set-url origin https://github.com/username/repo.git

# SSH 連線時，適當管理金鑰
ssh-keygen -t ed25519 -C "your.email@example.com"
```

#### 3. 歷史管理
```bash
# 刪除包含機密資訊的提交
git filter-branch --force --index-filter \
'git rm --cached --ignore-unmatch secrets/password.txt' \
--prune-empty --tag-name-filter cat -- --all
```

## 🚀 更新・擴充

### 📦 定期更新
```bash
# 檢查外掛更新
cd obsidian-multi-git-plugin
git pull origin main
npm run build

# 複製到 Obsidian 外掛資料夾
cp main.js ~/.obsidian/plugins/multi-git-manager/
```

### 🔧 自訂擴充
```typescript
// 新增獨自功能的範例
class CustomGitExtension {
    async autoCommit() {
        // 自動提交功能
    }
    
    async syncWithCloud() {
        // 雲端同步功能
    }
}
```

---

## 📞 支援・社群

### 🤝 需要協助時
- **GitHub Issues**: 錯誤回報・功能需求
- **Discussions**: 使用諮詢・想法分享
- **Wiki**: 詳細文件・FAQ

### 🎯 貢獻方法
- **錯誤回報**: 包含詳細的重現步驟
- **功能需求**: 說明具體的使用場景
- **程式碼貢獻**: 發送 Pull Request

---

**🎉 使用 Multi Git Manager 實現高效的 Git 工作流程！**

*🤖 Created with Claude Code integration by Lean consultant Futaro (OfficeFutaro)*