# Language / 言語 / 语言
**[English](../en/user-guide.md)** | **[日本語](../ja/user-guide.md)** | **[简体中文](../zh-CN/user-guide.md)** | **[繁體中文](../zh-TW/user-guide.md)**

---

# 📚 Multi Git Manager - 用户指南

## 🌟 概述

Multi Git Manager 是一个在 Obsidian 内高效管理多个 Git 仓库的插件。可以统一管理复杂的项目结构和多个笔记管理。

## 🎯 主要功能

### 🔍 自动仓库检测
- **Vault 本体**: Obsidian Vault 本身是 Git 仓库的情况
- **父目录**: Vault 外的项目根目录
- **子文件夹**: Vault 内的独立项目

### 📊 实时监控
- **状态栏显示**: 持续监控修改文件数量
- **30秒间隔更新**: 自动状态同步
- **可视化指示器**: 立即了解是否有变更

### 🎛️ 统一管理界面
- **Git Manager View**: 所有仓库的统一管理界面
- **批量操作**: 多个仓库的同时操作
- **选择性执行**: 仅针对需要的仓库

## 🖥️ 用户界面

### 1. 状态栏
```
[Git: 5 changes]  <-- 在这里显示
```
- **显示内容**: 所有仓库的修改文件总数
- **点击**: 打开 Git Manager View
- **更新频率**: 30秒间隔

### 2. 功能区图标
```
🌳 Git Manager  <-- 左侧栏的图标
```
- **功能**: Git Manager View 的开关
- **显示**: 始终可用
- **工具提示**: "Multi Git Manager"

### 3. 命令面板
`Ctrl/Cmd + P` 可使用的命令:

| 命令 | 功能 | 快捷键 |
|----------|------|----------------|
| **Open Git Manager** | 打开主管理界面 | - |
| **Show Git Status** | 详细状态显示 | - |
| **Git Commit** | 批量提交界面 | - |
| **Git Push** | 推送执行界面 | - |
| **Git Pull** | 拉取执行界面 | - |

## 📋 详细功能说明

### Git Manager View

#### 📊 仓库概览
```
┌─────────────────────────────────────┐
│ 🔄 Git Repository Manager          │
├─────────────────────────────────────┤
│ 📁 MyVault (3 changes)             │
│   • Modified: 2 files              │
│   • Untracked: 1 file              │
│   • Branch: main ↑1 ↓0             │
├─────────────────────────────────────┤
│ 📁 ParentProject (1 change)        │
│   • Modified: 1 file               │
│   • Branch: develop ↑0 ↓2          │
└─────────────────────────────────────┘
```

#### 🎮 控制按钮
- **🔄 Refresh**: 手动状态更新
- **📝 Commit All**: 所有仓库批量提交
- **⬆️ Push All**: 所有仓库推送
- **⬇️ Pull All**: 所有仓库拉取

### Git Status Modal

#### 📄 详细显示界面
```
┌─────────────────────────────────────┐
│ Git Status - All Repositories      │
├─────────────────────────────────────┤
│ 📂 MyVault                         │
│   Branch: main                     │
│   ↑1 ↓0                           │
│                                    │
│   Modified (2):                    │
│     M notes/daily/2023-10-24.md   │
│     M templates/meeting.md         │
│                                    │
│   Untracked (1):                   │
│     ? assets/image.png             │
├─────────────────────────────────────┤
│ 📂 ParentProject (Parent)          │
│   Branch: develop                  │
│   ↑0 ↓2                           │
│                                    │
│   Modified (1):                    │
│     M README.md                    │
└─────────────────────────────────────┘
```

### Git Commit Modal

#### ✏️ 提交创建界面
```
┌─────────────────────────────────────┐
│ Git Commit                          │
├─────────────────────────────────────┤
│ Select Repositories:                │
│   ☑️ MyVault - 3 changes           │
│   ☑️ ParentProject - 1 change      │
│   ☐ EmptyRepo - 0 changes          │
├─────────────────────────────────────┤
│ Commit Message:                     │
│ ┌─────────────────────────────────┐ │
│ │ Update daily notes and fix bugs │ │
│ │                                 │ │
│ │                                 │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│           [Commit] [Cancel]         │
└─────────────────────────────────────┘
```

### Git Push/Pull Modal

#### 🔄 同步操作界面
```
┌─────────────────────────────────────┐
│ Git Push                            │
├─────────────────────────────────────┤
│ Select Repositories:                │
│   ☑️ MyVault                       │
│   ☑️ ParentProject (Parent)        │
│   ☐ LocalOnlyRepo                  │
├─────────────────────────────────────┤
│             [Push] [Cancel]         │
└─────────────────────────────────────┘
```

## 🔧 高级使用方法

### 💼 工作流程示例

#### 1. 论文写作项目
```
📁 ResearchProject/
├── 🔄 obsidian-vault/ (备忘・想法)
├── 🔄 latex-manuscript/ (论文原稿)
├── 🔄 data-analysis/ (分析代码)
└── 🔄 bibliography/ (参考文献)
```

**运行流程**:
1. **日常工作**: 在各仓库进行独立工作
2. **阶段性提交**: 使用 Multi Git Manager 批量提交
3. **团队共享**: 选择性推送进行部分共享

#### 2. 博客文章管理
```
📁 BlogVault/
├── 🔄 drafts/ (草稿仓库)
├── 🔄 published/ (发布文章)
├── 🔄 assets/ (图像・资源)
└── 🔄 tools/ (自动化脚本)
```

**运行流程**:
1. **创建草稿**: 在 drafts/ 中写作文章
2. **审核**: 提交供团队确认
3. **发布准备**: 移动到 published/ 并整理
4. **批量部署**: 同步所有仓库

#### 3. 学习笔记管理
```
📁 StudyVault/
├── 🔄 computer-science/ (CS相关)
├── 🔄 mathematics/ (数学)
├── 🔄 projects/ (实习项目)
└── 🔄 resources/ (共享资源)
```

### 🎨 自定义设置

#### CSS 样式
```css
/* 自定义 styles.css */
.git-manager-view {
    font-family: 'JetBrains Mono', monospace;
}

.git-file-modified {
    color: #ff9500;
}

.git-file-added {
    color: #00ff00;
}

.git-file-deleted {
    color: #ff4757;
}
```

#### 自动更新间隔调整
```typescript
// main.ts 的相应部分
this.registerInterval(
    window.setInterval(() => this.updateStatusBar(), 10000) // 改为10秒
);
```

## 🔍 故障排除

### 常见问题及解决方法

#### 1. 🚫 仓库未被检测

**症状**: Git Manager View 中不显示仓库

**原因和解决方法**:
```bash
# 原因1: 未初始化为 Git 仓库
cd "[目标文件夹]"
git init

# 原因2: .git 文件夹被隐藏
ls -la  # 确认 .git 文件夹

# 原因3: 权限问题
chmod -R 755 .git
```

#### 2. ⚠️ Git 命令失败

**症状**: "git command not found" 错误

**解决方法**:
```bash
# Windows
where git
# 如果不显示路径，重新安装 Git for Windows

# macOS  
brew install git
# 或安装 Xcode Command Line Tools

# Linux
sudo apt install git  # Ubuntu/Debian
sudo yum install git   # CentOS/RHEL
```

#### 3. 🔄 状态不更新

**症状**: 修改文件后状态栏不更新

**解决方法**:
1. **手动更新**: 点击 Git Manager View 的🔄按钮
2. **重启插件**: 在设置中关闭插件后重新开启
3. **重启 Obsidian**: 完全重启

#### 4. 🚨 性能问题

**症状**: Obsidian 变慢

**原因**: 包含大量文件的仓库

**解决方法**:
```bash
# 在 .gitignore 中排除应该忽略的文件
echo "node_modules/" >> .gitignore
echo ".obsidian/workspace" >> .gitignore
echo "*.tmp" >> .gitignore
```

### 🛠️ 调试方法

#### 1. 开发者工具确认
```javascript
// 用 Ctrl+Shift+I 打开控制台
// 确认插件日志
console.log("Multi Git Manager"); // 用于搜索
```

#### 2. 启用详细日志
```javascript
// Obsidian设置 → 高级 → 开发者模式 设为开启
// 显示更详细的错误日志
```

#### 3. 文件权限确认
```bash
# 确认插件文件权限
ls -la .obsidian/plugins/multi-git-manager/
# 确认 main.js, manifest.json 可读取
```

## 📈 性能优化

### ⚡ 加速技巧

#### 1. 排除不必要的文件
```gitignore
# .gitignore 推荐设置
.obsidian/workspace
.obsidian/cache
*.tmp
*.log
node_modules/
.DS_Store
Thumbs.db
```

#### 2. 大型仓库分割
```
❌ 应避免的结构:
📁 HugeVault/ (10,000+ files)

✅ 推荐结构:
📁 MainVault/ (日常备忘)
📁 ArchiveVault/ (过去资料)
📁 ProjectVault/ (项目)
```

#### 3. 更新频率调整
- **重度用户**: 10秒间隔
- **普通用户**: 30秒间隔 (默认)
- **轻度用户**: 60秒间隔

## 🔐 安全考虑事项

### 🛡️ 安全运行

#### 1. 机密信息管理
```gitignore
# 排除机密文件
*.key
*.pem
.env
secrets/
private/
```

#### 2. 远程仓库设置
```bash
# 推荐 HTTPS 连接
git remote set-url origin https://github.com/username/repo.git

# SSH 连接时，适当管理密钥
ssh-keygen -t ed25519 -C "your.email@example.com"
```

#### 3. 历史管理
```bash
# 删除包含机密信息的提交
git filter-branch --force --index-filter \
'git rm --cached --ignore-unmatch secrets/password.txt' \
--prune-empty --tag-name-filter cat -- --all
```

## 🚀 更新・扩展

### 📦 定期更新
```bash
# 检查插件更新
cd obsidian-multi-git-plugin
git pull origin main
npm run build

# 复制到 Obsidian 插件文件夹
cp main.js ~/.obsidian/plugins/multi-git-manager/
```

### 🔧 自定义扩展
```typescript
// 添加独自功能的示例
class CustomGitExtension {
    async autoCommit() {
        // 自动提交功能
    }
    
    async syncWithCloud() {
        // 云同步功能
    }
}
```

---

## 📞 支持・社区

### 🤝 需要帮助时
- **GitHub Issues**: 错误报告・功能需求
- **Discussions**: 使用咨询・想法分享
- **Wiki**: 详细文档・FAQ

### 🎯 贡献方法
- **错误报告**: 包含详细的重现步骤
- **功能需求**: 说明具体的使用场景
- **代码贡献**: 发送 Pull Request

---

**🎉 使用 Multi Git Manager 实现高效的 Git 工作流程！**

*🤖 Created with Claude Code integration by Lean consultant Futaro (OfficeFutaro)*